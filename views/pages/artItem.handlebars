<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>{{artTitle}}</title>
  </head>
  <body>
    <main>
      <div>
        <h1>{{artTitle}}</h1>
        <p>By {{artist}}</p>
        <h2>Rating: {{rating}}</h2>
        <div>
          <img src="{{artImage}}" />
          <p>here is a super dope pic</p>
        </div>
        {{#if forSale}}
          <h2>${{setPrice}} USD</h2>
          <a href="{{purchasePage}}">Click here to purchase!</a>
        {{/if}}
        <div>
          <p>Genre: {{typeGenre}}</p>
          <h2>Comments:</h2>
        </div>
      </div>
    </main>
  </body>
</html>